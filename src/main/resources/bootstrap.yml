spring:
  application:
    name: programs
  cloud:
    config:
      enabled: false
      discovery:
        enabled: false
      uri: http://localhost:8888
      failFast: true
      retry:
        initialInterval: 3000
        multiplier: 1.3
        maxInterval: 10000
        maxAttempts: 15







